#include<iostream>


using namespace std;
int main()
{
    int a[]={12,2,35,17,8,10};
    int n=sizeof(a)/sizeof(a[0]);

    //Selection sort
    //TC->O(N*N)

    /*for(int i=0;i<n-1;i++)
    {
        int min=i;
        for(int j=i+1;j<n;j++)
        {
            if(a[j]<a[min])
            {
                min=j;
            }
            
        }
        if(min!=i)
        {
            swap(a[i],a[min]);
        }
        
    }*/


    
}
