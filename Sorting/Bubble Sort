#include<iostream>


using namespace std;
int main()
{
    int a[]={12,2,35,17,8,10};
    int n=sizeof(a)/sizeof(a[0]);

    //Bubble sort
    //Tc->O(N*N)

    /*for(int i=0;i<n-1;i++)
    {
        bool swapped = false;
        for(int j=0;j<n-i-1;j++)
        {
            if(a[j]>a[j+1])
            {
                swap(a[j],a[j+1]);
                swapped=true;
            }

        }
        if(swapped==false)
        break;
    }
    for(int i=0;i<n;i++)
    {
        cout<<a[i]<<" ";
    }*/


