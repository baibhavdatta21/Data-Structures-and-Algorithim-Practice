#include<iostream>


using namespace std;
int main()
{
    int a[]={12,2,35,17,8,10};
    int n=sizeof(a)/sizeof(a[0]);
    //Insersion Sort
    //TC->O(N*N)
    
    /*for(int i=1;i<n;i++)
    {
        int key=a[i];
        int j=i-1;

        while(j>=0 && a[j]>key)
        {
            a[j+1]=a[j];
            j=j-1;
        }
        a[j+1]=key;
    }*/
    
}
